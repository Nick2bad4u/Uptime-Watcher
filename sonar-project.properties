# SonarCloud configuration
sonar.organization=nick2bad4u
sonar.projectKey=Nick2bad4u_Uptime-Watcher
sonar.projectName=Uptime-Watcher
sonar.projectVersion=1.0

# SonarCloud server
sonar.host.url=https://sonarcloud.io

# Authentication (replace YOUR_TOKEN with actual token)
sonar.login=YOUR_TOKEN_HERE

# Source configuration
sonar.sources=src,electron
sonar.exclusions=**/node_modules/**,**/dist/**,**/dist-electron/**,**/build/**,**/coverage/**,**/release/**
sonar.typescript.file.suffixes=.ts,.tsx

# Test configuration (if you have tests)
# sonar.tests=src
# sonar.test.inclusions=**/*.test.ts,**/*.spec.ts

# Coverage (if you generate coverage reports)
# sonar.typescript.lcov.reportPaths=coverage/lcov.info

# connectedMode.json configuration export
# SonarCloud connected mode configuration
# {
#     "sonarCloudOrganization": "nick2bad4u",
#     "projectKey": "Nick2bad4u_Uptime-Watcher",
#     "region": "EU"
# }